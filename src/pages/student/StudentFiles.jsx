import React,{useEffect,useState}from'react';import{Link}from'react-router-dom';import{api}from'@/api/apiClient';import{useAuth}from'@/lib/AuthContext';import{gradeMatches}from'@/lib/grades';import PageHeader from'@/components/ui/PageHeader';import EmptyState from'@/components/ui/EmptyState';import{FileText}from'lucide-react';
export default function StudentFiles(){const{user}=useAuth();const[items,setItems]=useState([]);useEffect(()=>{Promise.all([api.entities.Material.list('-created_date',1000),api.entities.Course.list('-created_date',1000)]).then(([ms,cs])=>{const cm=new Map(cs.map(x=>[x.id,x]));setItems(ms.filter(x=>x.status==='published'&&gradeMatches(x.target_grade||cm.get(x.course_id)?.target_grade||'',user?.grade)))}).catch(console.error)},[user?.grade]);return <div dir="rtl"><PageHeader title="الملفات والملازم" description="كل الملفات التي نشرها المستر لصفك الدراسي"/>{!items.length?<EmptyState icon={FileText} title="لا توجد ملفات لصفك بعد"/>:<div className="grid gap-6 sm:grid-cols-2 xl:grid-cols-3">{items.map(x=><Link key={x.id} to={`/material/${x.id}?returnTo=${encodeURIComponent('/student/files')}`} className="course-reference-card"><div className="course-reference-media"><div className="content-icon-media"><FileText className="h-16 w-16 text-emerald-300"/><span className="accent-bar"/></div></div><div className="course-reference-body"><span className="course-reference-chip">ملف دراسي</span><h3 className="course-reference-title">{x.name||x.title}</h3><p className="course-reference-text">{x.description||'افتح الملف وشاهده من داخل المنصة.'}</p><div className="course-reference-footer"><span>منشور</span><span className="course-reference-action">فتح الملف ↗</span></div></div></Link>)}</div>}</div>}